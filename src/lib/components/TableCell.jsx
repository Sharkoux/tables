import React, { useState, useRef, useEffect } from "react"; const TableCell = ({ data, accessor }) => { const tData = data[accessor] ? data[accessor] : null; return ( <td className={`cell cell-${accessor}`} key={accessor}>{tData}</td> ) }; export default TableCell;
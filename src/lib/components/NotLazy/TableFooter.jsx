import React, { useEffect } from "react"; const TableFooter = ({ range, setPage, page, pagination, data }) => { useEffect(() => { if (page < 1) { setPage(1) } if (page >= range?.length) { setPage(range?.length) } }, [page, setPage, data]); return (<div > {pagination ? <div className="footer"> <button onClick={() => setPage(page - 1)}>Previous</button> <p>{page}</p> <button onClick={() => setPage(page + 1)}>Next</button> </div> : <></>} </div>) }; export default TableFooter
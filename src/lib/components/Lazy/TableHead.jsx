import React, { useState, useEffect } from "react"; const TableHeadItem = ({ onSort, accessor, item, sortable }) => { const [sortField, setSortField] = useState(null); const [sortOrder, setSortOrder] = useState(null); useEffect(() => { onSort({ sortField: sortField, sortOrder: sortOrder }) }, [sortField, sortOrder]); const handleSort = () => { if (!sortable) { return } setSortField(accessor); setSortOrder(sortOrder === 'asc' ? 'desc' : 'asc') }; return ( <td className={sortable ? `Columns Columns-${accessor} Columns-Sortable` : `Columns Columns-${accessor}`} title={item} onClick={handleSort} > {item} </td> ); }; export default TableHeadItem;
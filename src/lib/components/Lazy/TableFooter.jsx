import React, { useEffect, useState } from "react"; const TableFooter = ({ first, allUser, onPage, pagination, rows, page }) => { const [firsts, setFirst] = useState(first); const [pages, setPages] = useState(page); useEffect(() => { onPage({ first: firsts, page: pages }) }, [pages]); const handleNext = () => { if (page >= Math.ceil(allUser / rows)) { console.log('not'); return } setFirst(first + rows); setPages(page + 1) }; const handlePrevious = () => { if (pages <= 1) { return } setFirst(first - rows); setPages(page - 1) }; return (<div > {pagination ? <div className="footer"> <button onClick={handlePrevious}>Previous</button> <p>{page}</p> <button onClick={handleNext}>Next</button> </div> : <></>} </div>) }; export default TableFooter
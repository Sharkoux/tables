import React, { useState, useEffect, Component } from "react"; import TableCell from "./TableCell"; const TableRow = ({ data, Columns, index, onDragStart, onDragOver, onDrop, draggables }) => { const isDraggable = draggables === true; return ( <tr onDragStart={() => onDragStart(index)} onDragOver={() => onDragOver(index)} onDrop={onDrop} draggable={isDraggable} className={`rows rows-${index}`} > {Columns.map(({ accessor }) => { return <TableCell data={data} accessor={accessor} key={accessor} index={index} /> })} </tr> ); }; export default TableRow;